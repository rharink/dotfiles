{{ $sudo := "sudo " -}}
{{ if eq .chezmoi.username "root" -}}
{{   $sudo = "" -}}
{{ end -}}


#!/bin/bash

set -eufo pipefail

# Install Deps
{{ $sudo }}apt-get update
{{ $sudo }}apt-get install -y libxcb openssl-devel libX11-devel

# Install NuShell
cargo install nu --locked --features=dataframe
